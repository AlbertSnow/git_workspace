import android.media.MediaCodec.CodecException;

public final class id extends Exception
{
  public id(ir paramir, Throwable paramThrowable, int paramInt)
  {
    super(36 + String.valueOf(str1).length() + "Decoder init failed: [" + paramInt + "], " + str1, paramThrowable);
    if (paramInt < 0);
    for (String str2 = "neg_"; ; str2 = "")
    {
      int i = Math.abs(paramInt);
      new StringBuilder(64 + String.valueOf(str2).length()).append("com.google.android.exoplayer.MediaCodecTrackRenderer_").append(str2).append(i);
      return;
    }
  }

  public id(ir paramir, Throwable paramThrowable, String paramString)
  {
    super(23 + String.valueOf(paramString).length() + String.valueOf(str).length() + "Decoder init failed: " + paramString + ", " + str, paramThrowable);
    if ((ly.a >= 21) && ((paramThrowable instanceof MediaCodec.CodecException)))
      ((MediaCodec.CodecException)paramThrowable).getDiagnosticInfo();
  }
}

/* Location:           C:\Program Files\APK反编译\classes_dex2jar.jar
 * Qualified Name:     id
 * JD-Core Version:    0.6.0
 */